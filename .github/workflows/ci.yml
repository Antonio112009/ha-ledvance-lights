name: CI

# Branch flow: feature branches -> development -> main.
# PRs into either long-lived branch and pushes to them run the checks.
on:
  push:
    branches: [main, development]
  pull_request:
    branches: [main, development]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Ruff lint
        run: ruff check custom_components/ tests/ web/

      - name: Ruff format check
        run: ruff format --check custom_components/ tests/ web/

      - name: Run tests with coverage
        run: pytest --cov=custom_components/ha_ledvance_lights --cov-report=term-missing
